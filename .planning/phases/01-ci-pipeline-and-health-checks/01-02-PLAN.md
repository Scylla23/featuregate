---
phase: 01-ci-pipeline-and-health-checks
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: false
requirements:
  - CICD-01
  - CICD-02
  - CICD-03
  - CICD-04
  - CICD-05

must_haves:
  truths:
    - "Opening a PR triggers a GitHub Actions workflow that runs lint, typecheck, build, and tests"
    - "CI workflow uses pnpm 10.7.0, Node 20, and caches pnpm store"
    - "Concurrent runs for the same PR are cancelled to save resources"
    - "Branch protection requires CI to pass before merging"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline definition"
      contains: "pnpm lint"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pnpm lint"
      via: "workflow step"
      pattern: "pnpm lint"
    - from: ".github/workflows/ci.yml"
      to: "pnpm test"
      via: "workflow step"
      pattern: "pnpm test"
---

<objective>
Create a GitHub Actions CI workflow that validates every pull request with lint, build, typecheck, and test steps, then configure branch protection so PRs cannot merge without passing CI.

Purpose: Prevent broken code from landing on main. This is the quality gate that makes the rest of the deployment pipeline trustworthy.

Output: `.github/workflows/ci.yml` and branch protection configuration on the `main` branch.
</objective>

<execution_context>
@/Users/pavankushnure/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pavankushnure/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ci-pipeline-and-health-checks/01-RESEARCH.md

<interfaces>
<!-- Existing workflow patterns from the project -->

From .github/workflows/deploy-website.yml (established patterns):
- uses: pnpm/action-setup@v4
- uses: actions/setup-node@v4 with cache: 'pnpm'
- uses: actions/checkout@v4

From turbo.json (task pipeline):
- "lint": {} (no dependencies)
- "typecheck": { "dependsOn": ["^build"] }
- "build": { "dependsOn": ["^build"] }
- "test": {} (no dependencies defined)

From package.json (root scripts):
- "lint": "turbo run lint"
- "typecheck": "turbo run typecheck"
- "build": "turbo run build"
- "test": "turbo run test"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CI workflow file</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Create `.github/workflows/ci.yml` with the following configuration:

```yaml
name: CI

on:
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.7.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Build
        run: pnpm build

      - name: Typecheck
        run: pnpm typecheck

      - name: Test
        run: pnpm test
```

Key decisions per research:
- **Build before typecheck** because `@featuregate/evaluator` must be built for its `.d.ts` files to exist. Even though turbo.json has `typecheck.dependsOn: ["^build"]`, running build as an explicit prior step is safer in CI.
- **Single job** (not parallel matrix) because lint and typecheck are fast. A single job simplifies branch protection (one status check to require).
- **No services block** because all tests use `mongodb-memory-server` (in-process, no external containers needed).
- **concurrency with cancel-in-progress** to avoid wasting CI minutes on superseded pushes to the same PR.
- **pnpm 10.7.0** to match the project's `packageManager` field.
  </action>
  <verify>
    <automated>cd /Users/pavankushnure/dev/featuregate && cat .github/workflows/ci.yml | head -5 && echo "---" && grep -c "pnpm lint\|pnpm build\|pnpm typecheck\|pnpm test" .github/workflows/ci.yml</automated>
  </verify>
  <done>ci.yml exists with all 4 check steps (lint, build, typecheck, test), triggers on pull_request to main, uses pnpm 10.7.0 and Node 20.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify CI runs on GitHub and configure branch protection</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Push the branch to GitHub, open a PR against main to trigger CI, then configure branch protection to require the CI check.

What was built: CI workflow file at `.github/workflows/ci.yml` that runs lint, build, typecheck, and test on every PR to main.

Steps for the user:
1. Push the current branch to GitHub and open a PR against `main`. The CI workflow should trigger automatically.
2. In the PR's "Checks" tab, confirm these steps appear and pass: Lint, Build, Typecheck, Test.
3. Configure branch protection (CICD-05): Go to GitHub repo Settings > Branches > Add branch protection rule. Branch name pattern: `main`. Check "Require status checks to pass before merging". Search for and select the `ci` status check. Save.
4. Verify protection works: The PR should show "merging is blocked" until CI passes.
  </action>
  <verify>
    <automated>cd /Users/pavankushnure/dev/featuregate && test -f .github/workflows/ci.yml && echo "ci.yml exists" && grep -q "pull_request" .github/workflows/ci.yml && echo "triggers on PR"</automated>
  </verify>
  <done>CI workflow runs successfully on a GitHub PR with all 4 steps passing. Branch protection on main requires CI to pass before merging. User has confirmed by typing "approved".</done>
</task>

</tasks>

<verification>
- `.github/workflows/ci.yml` exists and is valid YAML
- Workflow triggers on `pull_request` to `main`
- All 4 steps present: lint, build, typecheck, test
- Branch protection configured on `main` requiring CI to pass
</verification>

<success_criteria>
- Opening a PR triggers the CI workflow
- CI workflow runs lint, build, typecheck, and test steps sequentially
- CI uses pnpm 10.7.0 with caching and Node 20
- Branch protection on main requires CI to pass before merging
</success_criteria>

<output>
After completion, create `.planning/phases/01-ci-pipeline-and-health-checks/01-02-SUMMARY.md`
</output>
